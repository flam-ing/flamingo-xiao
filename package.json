{
  "name": "flamingo-xiao",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Flamingo Xiao: original ink-fighter martial arts across a giant computer desk",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4207 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4207 --strictPort",
    "test": "node --test tests/*.test.mjs",
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "@napi-rs/canvas": "0.1.80",
    "galmuri": "2.40.3"
  }
}
